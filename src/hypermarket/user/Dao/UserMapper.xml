<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hypermarket.user.Dao.UserMapper">

	<select id="getUserByUserNameAndPassword"  resultType="hypermarket.user.bean.Users">
		select * from backuser where userName=#{userName} and password=#{password}
	</select>
</mapper>